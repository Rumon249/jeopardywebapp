The language (abbreviation) used to create web pages,HTML,100
The type of memory (full name) that stores running programs,Random Access Memory,200
Performs arithmetic operations on a computer,Arithmetic Logic Unit,300
The type of code that computers read to carry out instruction,Machine Code,400
Changing a representation to use fewer bits to store information,Data Compression,500
User interface (abbreviation) that uses icons and a mouse,GUI,600
Circuit that carries data from one area to another,Data Bus,700
Taking measurements at regular intervals when digitizing sound,Sampling,800
